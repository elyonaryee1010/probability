==Probability==
def pick_ball_experiment():
    balls = ['Red', 'Blue', 'Orange', 'Grey', 'Pink']

result = random.choice(balls)

pro = balls.count('red')/len(balls)
print("Probability of Picking Red Ball")

if result == 'Red':
    return 'Red Ball was Picked'
else:
    return 'Better Luck Next Time'
    
    print(pick_ball_experiment)

user_input = input("Enter guess")

set1 = {'A', 'B', 'C'}
set2 = {'D', 'E', 'F'}

union = user_input

pro=balls()/len(balls)

set_union1 = union(set1)
set_union2 = union(set2)
user_input = guess
guess = list(guess)


print(set_union1)
print(set_union2)
print("Union:", union)
print("Guess:", gues)


def probability():
    probability = len(user_input)
    intersection = (set_union1 + set_union2 - intersection)
    print(intersection)
    
    even = {2,4,6,8,10}
    odds = {1,3,5,7,9}
    print("Probability:", probability)


==Conditional Probability==
def a_and_b():
    if a==1:
        prob_student = 2.3
    if b==1:
        prob_dinning = 0.75
    else:
        prob_dinning = 0.25
        print("Probability of given data", a_and_b)

    if a==2:
        prob_student = 0.7
    if b==1:
        prob_dining = 0.8
    else:
    prob_dining = 0.4


print("", a_and_b)
prob_a_and_b = prob_student*prob_dinning
return round(prob_a_and_b)

print("Check the probability")

print("Is the student a Freshman or not?")
a = int(input("Enter your answer"))

print("Is the student at the dining table")
b= int(input("Enter your answer")

print("Here is the probability", prob_a_and_b(a,b))


==Multiplication Method==

def prob_a_andb(a,b):
    prob_a = orange/total

    prob_bga = blue/(total)

    prob_AandB = prob_a*prob_b

return round(prob_AandB)

orange = int(input("Enter:"))
blue = int(input("Enter:"))
total = orange+blue


print("Probability")
    print(prob_a_andb(a,b))

==Pick a Shirt==
print("Find the probability of 2 shirts you pick")



first_blue_pick = int(input("Enter First Pick"))



    if first_pick==1:
        next_shirt_prob == 1.7
    else:
        next_shirt_prob ==0.7
    print("======")

total = next_shirt_prob+first_pick
    next_shirt_is_red == first_pick/total

#Finding if the probability of if the 1st pick is red, and the second shirt is blue
red_shirts = 2
blue_shirts = 4
white_shirts = 9
total = 3

total_probability = red_shirts*blue_shirts*white_shirts

probability1 = print(red_shirts/total)
probability2 = print(blue_shirts/total)
probability3 = print(white_shirts/total)

#CoinCDF
import scipy.stats as stats

prob = 1- stats.binom.cdf(6,10,0.5)
print("Probability of getting more than 4 heads", prob)

#Rain Expectation

import scipy.stats as stats

prob1 = stats.poisson.pmf(6,10)
print("Probability of raining for exactly 6 days", prob1)

prob2 = stats.poisson.pmf(12,10) + stats.poisson.pmt(13,10) + stats.poisson.pmf(14,10)
print("Probability of raining for 12-14 days:", prob2)

 #Calls Poisson   

import scipy.stats as stats

prob1 = stats.poisson.pmf(20,15)
print("Probability of observing morethan 20calls?", prob1)

prob2 = stats.poisson.pmf(17,15) + stats.poisson.pmf(18,15)
+ stats.poisson.pmf(19,15) + stats.poisson.pmf(20,15) + stats.poisson.pmf(21,15)
print("Probability of observing between 17 to 21 calls", prob2)



        
    
    
    

    
